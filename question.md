# 과제 평가 대비 질의응답 정리 (Q&A)

> 본 문서는 `travel_planner.py`의 구현 내용과 과제 목표를 바탕으로, 평가 및 코드 리뷰 시 쉽게 답변하고 설명할 수 있도록 정리한 질의응답 가이드입니다.

---

# 항목 1. 기본 기능 구현

## 1-1. date 옵션으로 프로그램을 실행할 수 있으며, 날짜 형식 오류 시 사용법을 출력하고 종료하는가?
* **답변**:
  * 파이썬 표준 라이브러리인 `argparse`를 활용하여 `-date` 및 `--date` 옵션을 필수 인자로 등록했습니다.
  * 입력된 날짜는 정규표현식(`r"^\d{4}-\d{2}-\d{2}$"`)과 `datetime.strptime()` 함수를 통해 실제 달력에 존재하는 올바른 날짜인지 2중으로 엄격하게 검증합니다.
  * 만약 날짜 형식이 올바르지 않거나 존재하지 않는 날짜(예: 2026-02-30)인 경우, 콘솔에 명확한 오류 안내와 함께 프로그램 사용법(`parser.print_usage()`)을 출력하고 `sys.exit(1)`로 즉시 안전하게 종료합니다.

## 1-2. 1차 LLM 응답이 JSON으로 파싱되며 필수 키가 모두 존재하는가?
* **답변**:
  * Google Gemini API 호출 시 `generationConfig`에 `"responseMimeType": "application/json"`을 명시하여 LLM이 일반 텍스트나 사족 없이 100% 순수 JSON으로만 응답하도록 강제했습니다.
  * 응답 텍스트를 `json.loads()`로 파싱한 후, 가이드에 명시된 필수 키(`recommended_city`, `weather`, `events`, `reason`)가 모두 존재하는지 유효성을 검사합니다.
  * 추가로 보너스 과제인 복수 지역 추천을 위한 `recommended_cities` 배열도 함께 검증하여 데이터의 완결성을 확보했습니다.

## 1-3. 1차 결과의 추천 지역을 입력으로 지도/장소 API를 호출하는가?
* **답변**:
  * 1차 LLM의 JSON 출력에서 추출한 도시명(`recommended_city` 및 `recommended_cities` 목록)을 카카오 로컬 검색 API의 입력 쿼리로 직접 연결했습니다.
  * `f"{city} 맛집"` 형식의 키워드를 만들고, 음식점 전용 카테고리 코드(`category_group_code=FD6`)를 지정하여 해당 지역의 실제 맛집 5곳에 대한 상세 정보(식당명, 도로명 주소, 카테고리, 좌표, 카카오맵 링크)를 수집합니다.

## 1-4. results/에 원본 JSON과 최종 Markdown 리포트가 저장되는가?
* **답변**:
  * `StorageManager` 클래스를 구현하여 실행 시점에 `results/` 폴더가 없으면 자동으로 생성(`mkdir(parents=True, exist_ok=True)`)하도록 처리했습니다.
  * 실행 후에는 실행 일자를 파일명 접두어로 사용하여 아래 2가지 파일이 항상 안전하게 저장됩니다:
    1. **원본 데이터 JSON** (`results/{date}_travel_data.json`): 1차 추천 데이터, 수집된 지역별 맛집 목록, 오류 내역(`errors`)
    2. **최종 여행 리포트 Markdown** (`results/{date}_travel_plan.md`): 사용자 맞춤형 1일 일정 및 맛집이 포함된 종합 마크다운 리포트

## 1-5. 코드/결과물/README 어디에도 API 키가 직접 포함되어 있지 않은가?
* **답변**:
  * 모든 API 키(`GEMINI_API_KEY`, `KAKAO_REST_API_KEY`)는 로컬의 `.env` 파일 또는 시스템 환경변수를 통해서만 주입되도록 구현했습니다.
  * `.gitignore` 파일에 `.env` 및 `.env.*`를 등록하여 Git 커밋이나 원격 저장소 푸시 시 API 키가 노출되는 사고를 원천 차단했습니다.
  * 공유용 문서인 `README.md`와 `.env.example`에는 실제 키 대신 형식 안내용 플레이스홀더(`AIzaSy...`, `1234abcd...`)만 기재했습니다.

---

# 항목 2. 코드 구조 및 설계

## 2-1. "1차 추천 생성 → 맛집 검색 → 최종 리포트 생성" 흐름을 함수/모듈로 어떻게 분리했는가?
* **답변**:
  * **단일 책임 원칙(SRP)**에 따라 기능별로 클래스와 함수를 깔끔하게 분리했습니다:
    1. `GeminiClient.generate_travel_recommendation()`: 1단계 날짜 기반 지역/날씨/축제 정보 JSON 구조화 생성 담당
    2. `KakaoLocalClient.search_restaurants()`: 2단계 추천 도시별 맛집 검색 및 정형 데이터 변환 담당
    3. `GeminiClient.generate_final_report()`: 3단계 1차 추천과 맛집 데이터를 조합한 최종 마크다운 리포트 생성 담당
    4. `StorageManager`: 파일 저장, 디렉터리 관리, 캐싱 입출력 전담
    5. `main()`: 전체 파이프라인의 순서 제어, 입력 검증, 진행 상황 로깅 총괄

## 2-2. 1차 JSON 스키마를 코드에서 어떻게 검증했는가?
* **답변**:
  * `json.loads()`로 파싱된 딕셔너리에 필수 키(`recommended_city`)가 존재하는지 확인합니다.
  * 만약 복수 지역 추천 목록(`recommended_cities`)만 반환된 경우, 첫 번째 요소의 도시명을 대표 도시로 자동 보정하는 방어 코드를 적용했습니다.
  * 반대로 단일 도시만 반환된 경우에도 `recommended_cities` 배열 형태로 감싸주어 이후 단계의 반복문(루프)이 에러 없이 일관되게 동작하도록 보장했습니다.

## 2-3. 지도/장소 API 제공자를 바꾸더라도 변경 범위를 최소화하기 위해 어떤 구조로 설계했는가?
* **답변**:
  * **어댑터(Adapter) 패턴**과 같은 인터페이스 분리 방식으로 설계했습니다.
  * `KakaoLocalClient` 내부에서 카카오 고유의 응답 필드(`documents`, `place_name`, `road_address_name` 등)를 표준 공통 규격(`name`, `address`, `category`, `url`, `x`, `y`)으로 변환하여 반환하도록 캡슐화했습니다.
  * 따라서 향후 네이버나 구글 지도 API로 변경하더라도, 메인 로직(`main()`)이나 리포트 생성기는 전혀 수정할 필요 없이 새로운 클라이언트 클래스를 추가하고 동일한 공통 규격으로 매핑만 해주면 됩니다.

## 2-4. 에러를 errors 목록으로 누적하고 최종 리포트에 반영하는 방식은?
* **답변**:
  * API 호출 과정에서 발생하는 예외(예: 검색 결과 0건, 인증 실패 401 등)를 즉시 프로그램을 중단시키지 않고, 구조화된 에러 객체(`{"step": "place_search", "city": city, "type": "...", "message": "..."}`)로 만들어 `errors` 리스트에 차곡차곡 누적(`errors.append()`)합니다.
  * 이 누적된 `errors` 리스트는 원본 JSON에 그대로 보존되며, 3단계 리포트 생성 프롬프트로 전달되어 마크다운 리포트의 `## 오류 요약(errors)` 섹션에 깔끔하게 정리되어 출력됩니다.

---

# 항목 3. API 및 데이터 처리 이해

## 3-1. GET/POST 중 무엇을 어디에 사용했으며, 그 이유는?
* **답변**:
  * **Kakao Local API (GET 사용)**:
    - **목적**: 이미 존재하는 장소/음식점 정보의 '조회'.
    - **이유**: 검색어와 파라미터가 단순하며, URL 쿼리 스트링(`?query=...&size=5`)으로 전달하는 것이 HTTP 표준에 부합하고 브라우저나 프록시의 캐싱을 활용할 수 있기 때문입니다.
  * **Google Gemini API (POST 사용)**:
    - **목적**: 프롬프트 전달 및 텍스트 '생성 요청'.
    - **이유**: 전달하는 프롬프트 내용과 스키마 명세가 길고 복잡하므로, URL 길이 제한이 없고 데이터를 안전하게 전송할 수 있는 HTTP 요청 본문(Request Body)에 JSON 형식으로 담아 보내야 하기 때문입니다.

## 3-2. LLM 출력 형식을 자유 텍스트가 아니라 JSON으로 강제한 이유와 장점은?
* **답변**:
  * **이유**: LLM이 생성한 결과 데이터를 다음 프로그램 단계(지도 API 호출)의 정확한 입력값으로 사용하기 위함입니다.
  * **장점**:
    1. **기계 가독성(Machine-readable)**: 자유 텍스트는 불필요한 미사여구나 서론/결론이 섞여 도시명을 정확히 추출하기 어렵지만, JSON은 `data["recommended_city"]`와 같이 키 기반으로 100% 명확하게 접근할 수 있습니다.
    2. **파이프라인 자동화**: 1단계(LLM) → 2단계(지도 API) → 3단계(리포트)로 이어지는 데이터 파이프라인의 연결을 오류 없이 견고하게 자동화할 수 있습니다.

## 3-3. 외부 API에서 401/403이 발생하는 대표 원인과 디버깅 순서는?
* **답변**:
  * **대표 원인**:
    - **401 (Unauthorized)**: API 키 누락, 오타, 잘못된 헤더 명칭 및 인증 접두사(예: `KakaoAK ` 미포함).
    - **403 (Forbidden)**: 권한 부족, 허용되지 않은 IP/도메인에서의 요청, 해당 서비스 활성화 미신청.
  * **디버깅 순서**:
    1. `.env` 파일에 작성된 API 키 값의 앞뒤 공백이나 따옴표 오기입 여부를 확인합니다.
    2. 공식 API 문서와 비교하여 HTTP 헤더 이름(`Authorization`) 및 접두어 포맷이 정확한지 점검합니다.
    3. 개발자 콘솔(카카오/구글)에 접속하여 발급받은 키의 상태(활성화 여부, 쿼터 제한, 플랫폼 허용 설정)를 점검합니다.

## 3-4. API 키를 .env / 환경변수로 관리해야 하는 이유를 보안/운영 관점에서 설명할 수 있는가?
* **답변**:
  * **보안 관점**:
    - 소스 코드를 GitHub 등 공용 저장소에 공유하거나 협업할 때 비밀 키가 함께 유출되는 보안 사고를 원천 방지합니다.
    - 유출로 인한 악의적인 쿼터 남용 및 무단 과금 피해를 예방합니다.
  * **운영 관점**:
    - 로컬 개발 환경, 테스트 환경, 운영 서버마다 서로 다른 API 키를 소스 코드 수정 없이 유연하게 교체할 수 있습니다.
    - 키가 만료되거나 재발급받을 때도 코드를 다시 빌드하거나 배포할 필요 없이 설정값만 바꿔주면 되므로 운영 효율성이 극대화됩니다.

---

# 항목 4. 예외 상황 및 개선 방안

## 4-1. LLM이 JSON을 깨뜨린 출력(불완전 JSON)을 내보낼 때 어떻게 처리했는가?
* **답변**:
  * 1차적으로 Gemini API의 `responseMimeType: "application/json"` 기능을 활성화하여 모델 차원에서 유효한 JSON 형식만 출력하도록 유도했습니다.
  * 만약 모델이 마크다운 코드블록(````json ... ````)을 감싸서 반환하는 경우, 정규표현식으로 코드블록을 깔끔하게 제거한 뒤 파싱하도록 전처리했습니다.
  * 그럼에도 파싱 에러가 발생하면, "앞선 응답 파싱에 실패했으니 반드시 유효한 JSON 형식만 출력하라"는 수정 프롬프트를 주입하여 **최대 1회 재시도**를 수행하도록 구현했습니다.

## 4-2. 지도/장소 검색 결과가 0건일 때 어떤 정책을 선택했는가?
* **답변**:
  * **결함 격리(Fault Isolation)** 정책을 선택했습니다.
  * 검색 결과가 0건이어도 프로그램을 강제 종료하지 않고, `errors` 리스트에 `EMPTY_RESULT`를 기록한 뒤 빈 리스트(`[]`)를 반환하여 다음 단계로 정상 진행시킵니다.
  * 최종 리포트의 맛집 섹션에는 `- 데이터 없음 (장소 검색 결과 0건)`으로 명확히 표기하여, 추천 지역 소개와 날씨, 행사 등 유용한 나머지 정보들은 사용자에게 정상적으로 제공되도록 구현했습니다.

## 4-3. 같은 날짜를 여러 번 실행할 때 API 호출 비용을 줄이기 위한 방법은?
* **답변**:
  * **로컬 파일 기반 결과 캐싱(보너스 과제 2)** 메커니즘을 구현했습니다.
  * 동일한 날짜(`-date`)로 실행 시, 이미 `results/{date}_travel_data.json`이 존재하는지 먼저 확인합니다.
  * 캐시 파일이 존재하면 외부 API(Gemini 추천, Kakao 맛집 검색)를 중복 호출하지 않고 기존 저장된 JSON 데이터를 즉시 로드하여 최종 리포트를 생성합니다.
  * 이를 통해 네트워크 지연을 없애고, 불필요한 API 토큰 낭비 및 호출 비용을 획기적으로 절감했습니다. (필요 시 `--no-cache` 옵션으로 언제든 최신 데이터 갱신 가능)

## 4-4. 추천 도시가 너무 넓거나 애매한 경우 어떻게 검색 품질을 높일 수 있는가?
* **답변**:
  * **프롬프트 고도화 (LLM 제어)**:
    - 프롬프트에 "대한민국 행정구역 상 '시/군' 단위(예: '강원도' 대신 '강릉시', '전라남도' 대신 '순천시')로 구체화하여 추천할 것"을 명시하여 광역 지자체나 모호한 지역명이 도출되지 않도록 통제합니다.
  * **검색 파라미터 정밀화 (지도 API)**:
    - 추천된 도시의 중심 좌표(위도/경도)를 기준으로 반경(radius) 제한 검색을 적용하거나, 검색어 키워드를 `"{city} 대표 명소 맛집"` 또는 `"{city} 특산물 향토음식점"` 등으로 구체화하여 검색 결과의 정확도와 품질을 끌어올릴 수 있습니다.